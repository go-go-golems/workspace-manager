# workspace-manager xgoja provider smoke

Builds a generated xgoja binary that imports `github.com/go-go-golems/workspace-manager/pkg/wsmjs/provider`, selects module `wsm`, and runs a non-destructive API-shape smoke script.

```bash
make smoke
```
